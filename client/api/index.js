import request from "superagent"

export function searchImdb (arr) {
  // return request.get(imdbUrl)
  //   .then(res => res.body)
  //   .catch(err => {
  //     throw Error(err.message)
  //   })
}
